export HTMD_NONINTERACTIVE=1
. /mnt/scratch/shared/miniconda/bin/activate htmd
alias nb='jupyter notebook --no-browser --ip=159.149.160.118'

echo "

Environment for the Markov Modeling exercises initialized.
"


dir=$HOME/markov-practice

if [[ ! -d $dir ]] ; then
	cp -a ~tonigiorgino/practice $dir 
	echo "A directory with the exercise templates and data files
was copied in $dir"
else
	echo "Not overwriting $dir"
fi


cat <<EOF

When instructed:
* _R_ starts an R interpreter. 
* _nb_ starts a jupyter notebook (then open the link printed)."

EOF


